// Twitter/X uses the same card art as Open Graph. Re-export the generator so
// there's a single source of truth for the social image.
export { default, alt, size, contentType } from "./opengraph-image";
